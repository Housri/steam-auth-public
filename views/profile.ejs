<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= user.username %> - Profile</title>
    <style>
        body { 
            font-family: Arial, sans-serif; 
            background: #1b2838; 
            color: white;
            text-align: center;
            padding: 2rem;
        }
        .profile-card {
            background: #2a475e;
            padding: 3rem;
            border-radius: 15px;
            max-width: 400px;
            margin: 2rem auto;
            border: 1px solid #66c0f4;
        }
        .avatar {
            width: 150px;
            height: 150px;
            border-radius: 50%;
            border: 4px solid #66c0f4;
            margin-bottom: 1rem;
        }
        .username {
            font-size: 1.5rem;
            color: #66c0f4;
            margin-bottom: 0.5rem;
        }
        .steam-id {
            font-size: 1rem;
            opacity: 0.8;
            margin-bottom: 1rem;
        }
        .btn {
            background: #66c0f4;
            color: #1b2838;
            padding: 10px 20px;
            text-decoration: none;
            border-radius: 4px;
            display: inline-block;
            margin: 0.5rem;
        }
    </style>
</head>
<body>
    <div class="profile-card">
        <img src="<%= user.avatar.large %>" alt="Profile" class="avatar">
        <h1 class="username"><%= user.username %></h1>
        <p class="steam-id">SteamID64: <%= user.steamId %></p>
        <a href="<%= user.profileUrl %>" target="_blank" class="btn">View Steam Profile</a>
        <a href="/" class="btn">Home</a>
        <a href="/logout" class="btn" style="background: #ff4444;">Logout</a>
    </div>
</body>
</html>
